<?php
/** @var \KW\CustomerWallet\Block\Account\Wallet\WalletTransfer $block */
?>

<div class='block block-dashboard-info'>
    <div class='block-content'>
        <form class='form form-wallet-transfer'
              action="<?= $block->escapeUrl($block->getPostActionUrl()) ?>"
              method='post'
              id='wallet-transfer-form'>

            <?= $block->getBlockHtml('formkey'); ?>

            <fieldset class="fieldset info">
                <legend class="legend"><span><?= __('Wallet Transfer') ?></span></legend>
                <br>

                <!-- Receiver Public Hash -->
                <div class="field required">
                    <label class="label" for="receiver_public_hash">
                        <span><?= __('Receiver Public Hash') ?></span>
                    </label>
                    <div class="control">
                        <input type="text"
                               name="receiver_public_hash"
                               id="receiver_public_hash"
                               value=""
                               title="<?= __('Receiver Public Hash') ?>"
                               class="input-text"
                               data-validate="{required:true}" />
                        <div class="note">
                            <?= __('Please make sure the hash is correct. Transfers cannot be reversed once processed.') ?>
                        </div>
                    </div>
                </div>

                <!-- Amount -->
                <div class='field required'>
                    <label class='label' for='amount'>
                        <span><?= __('Amount') ?></span>
                    </label>
                    <div class="control">
                        <input type="number"
                               step="0.01"
                               name="amount"
                               id="amount"
                               value=""
                               min="0.01"
                               title="<?= __('Amount') ?>"
                               class="input-text"
                               data-validate="{required:true, 'validate-number':true}"/>
                        <div class="note">
                            <?= __('Enter the amount to transfer (decimals allowed).') ?>
                        </div>
                    </div>
                </div>

                <!-- Note -->
                <div class="field">
                    <label class="label" for="note">
                        <span><?= __('Note') ?></span>
                    </label>
                    <div class="control">
                        <textarea name="note"
                                  id="note"
                                  title="<?= __('Note') ?>"
                                  class="input-text"
                                  rows="4"></textarea>
                    </div>
                </div>
            </fieldset>

            <div class="actions-toolbar">
                <div class="primary">
                    <button type="submit"
                            class="action save primary">
                        <span><?= __('Transfer Money') ?></span>
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>
